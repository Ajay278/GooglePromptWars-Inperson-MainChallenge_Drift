# Copy this file to .env. Never commit or share the .env file.
# Vertex AI authenticates with Application Default Credentials or the deployed
# service account; no Gemini API key is needed.
GOOGLE_CLOUD_PROJECT=drift-502807
GOOGLE_CLOUD_LOCATION=global
